/**
 * Web arayüz sürümü — her Vercel yayınından önce artırın.
 * Örnek: 1.2.1 → 1.2.2 (küçük düzeltme), 1.2.x → 1.3.0 (yeni modül)
 */
export const APP_VERSION = '1.2.1'

export const APP_VERSION_LABEL = `v${APP_VERSION}`
